@model IEnumerable<OdeToFood.Data.Models.Restaurant>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var item in Model)
{
    @Html.Partial("_RestaurantSummary", item)
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>


@section footer {
    <div>This is a custom footer section</div>
}
